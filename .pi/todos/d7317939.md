{
  "id": "d7317939",
  "title": "FR stage A: configurable locale pair + fr scaffolding (architect)",
  "tags": [
    "i18n",
    "fr",
    "stage-A"
  ],
  "status": "completed",
  "created_at": "2026-09-11T15:56:43.098Z"
}

Architect (zai/glm-5.3): GEV_DEFAULT_LOCALE + GEV_SECONDARY_LOCALE env vars → vite.config import.meta.env defines → locale.js pair resolution (en always source/fallback; unsupported/dupe pair → en+es); dynamic selector rendering from pair in ui.js (replaces static EN|ES buttons; markupCoverage test updated); fr normalization (fr/fr-* → fr); fr catalog scaffolds (en values + UNTRANSLATED seed); parity gate generalized to ALL shipped locales (es+fr); .env.example entries. Main checkout, explicit-path adds, bash rg only (no dedicated grep tool).
